name: flutter_llamacpp_example
description: Example app demonstrating the flutter_llamacpp package.

publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_llamacpp:
    path: ../
  http: ^1.2.0
  path_provider: ^2.1.0
  file_picker: ^8.0.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
